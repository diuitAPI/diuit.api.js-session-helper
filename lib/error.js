var Error = {
    VALUE_EMPTY : ' is empty',
    IS_NOT_STRING: ' is not string',
    IS_NOT_NUM: ' is not number',
    INVALID_PLATFORM: ' is invalid platform',
    SUPPORT_PLATFORM: 'only support : '
};

module.exports = Error;
